# タイクーンゲーム開発プロジェクト — Claude Code 運用指針

このファイルはClaude Codeが毎セッション自動的に読み込む。作業を始める前に必ず全体を把握すること。

## プロジェクト概要

2Dタイクーンゲームを開発する。

- 自動的に集まる資源(コイン)を核とした放置ゲーム的な収集ループ
- 施設の強化(コストと効果が段階的に上昇)
- 豚(コレクションアイテム)を集める要素
- UIは2D(React)を基本とする。牧場ビューの3D化はフェーズ8(拡張)で実施

**技術スタック**: React + TypeScript + Vite + Zustand + Vitest

## 開発フロー(フェーズゲート方式)

このプロジェクトは以下5フェーズで進行する。**各フェーズの成果物が完成したら必ず立ち止まり、
ユーザーの明示的な承認を得てから次フェーズのコードを書き始めること。** これは自律開発の速度を
落とすためではなく、「数値バランス」や「仕様の勘違い」はコードを書いてからでは手戻りコストが
大きいため、設計段階で人間のレビューを挟む方が結果的に速いという判断による。

| # | フェーズ | 対応スキル | 主な成果物 |
|---|---|---|---|
| 1 | 要件定義 | `phase-1-requirements` | `docs/requirements.md` |
| 2 | 状態設計 | `phase-2-state-design` | `src/types/`, `src/store/`(スケルトン), `docs/state-design.md` |
| 3 | コアロジック実装 | `phase-3-core-logic` | `src/logic/` + テスト一式 |
| 4 | UI実装(2D) | `phase-4-ui-implementation` | `src/components/` |
| 5 | テスト・バランス調整 | `phase-5-testing-balance` | 調整済みパラメータ、既知課題一覧 |
| 6 | 実績システム(拡張) | `phase-6-achievements` | `src/logic/achievements.ts` + テスト、実績UI |
| 7 | プレステージ(拡張) | `phase-7-prestige` | `src/logic/prestige.ts` + テスト、転生UI |
| 8 | 3D化(拡張) | `phase-8-3d-view` | React Three Fiberによる牧場ビューの3D差し替え |
| 9 | 3Dワールド化(拡張) | `phase-9-3d-world` | キャラクター操作型の3Dワールド(Robloxタイクーン風) |
| 10 | ワールド拡張(拡張) | `phase-10-world-expansion` | 飼育場・転生の祠・コイン山・装飾オブジェクト・ワールド拡大 |
| 11 | モバイル対応(拡張) | `phase-11-mobile-support` | バーチャルスティック・タッチ操作・レスポンシブ調整 |

フェーズ6〜8は2026-07-17にユーザーの依頼で追加した拡張フェーズ(フェーズ5承認後のスコープ追加)。
フェーズ2以降の承認ゲートはユーザーの明示的な指示(2026-07-15)により免除され、自律進行している。

各フェーズの詳細な進め方・完了条件・Gitアクションは `.claude/skills/phase-N-xxx/SKILL.md` を参照。

## 承認ゲートのルール

1. フェーズ内のタスクは自律的に進めてよい(承認を都度求めない)
2. フェーズが完了したら、変更点のサマリー・次フェーズへの引き継ぎ事項・確認したい点を提示して**作業を止める**
3. 「進めてください」「承認」「OK」など明確な合図があるまで次フェーズのコードは書かない
4. 承認前に修正指示が来た場合は同一フェーズ内で反映し、再度サマリーを提示して確認を求める
5. フェーズをまたぐ判断(例: 仕様変更が前フェーズの成果物に影響する)は必ず確認を取ってから進める

## Gitワークフロー

詳細は `.claude/skills/git-workflow/SKILL.md` を参照。要点のみ:

- フェーズごとに `phase/N-slug` ブランチを切る(例: `phase/1-requirements`)
- 意味のある単位でこまめにコミット・こまめにプッシュする(フェーズ完了までためない)
- フェーズ完了後に `master` へマージし、次フェーズのブランチを作る(メインブランチは `main` ではなく `master`)
- `master` への直接pushや、フェーズ途中のマージは行わない

## コーディング規約

- ロジック層(`src/logic/`, `src/store/`)とUI層(`src/components/`)を明確に分離する。将来UIをReact Three Fiberに差し替える可能性があるため、ロジックがUIフレームワークに依存しないようにする
- 型はすべてTypeScriptで明示する(`any`禁止)
- コアロジック(資源計算・コスト曲線・収集判定)には必ずVitestでユニットテストを書く
- マジックナンバーは `src/logic/constants.ts` に集約し、コード中に直接埋め込まない(フェーズ5でのバランス調整を容易にするため)

## 現在の進行状況の確認方法

セッション開始時、以下の順で状況を把握してから作業を始めること:

1. `git branch` で現在のブランチを確認し、どのフェーズにいるか判断する
2. `docs/` 配下に完了済みフェーズの成果物があるか確認する
3. 直近のコミットログ(`git log --oneline -10`)で未完了のタスクがないか確認する
4. 状況が不明な場合は推測で進めず、ユーザーに現在のフェーズを確認する
